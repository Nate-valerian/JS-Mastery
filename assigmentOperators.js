let number = 5;

number += 5; // the same as number = number + 5;
number -= 5; // the same as number = number - 5;
number *= 5; // the same as number = number * 5;
number /= 5; // the same as number = number / 5;
console.log(number);

let string = 'Hello';
string += ', I am John.';
console.log(string);
